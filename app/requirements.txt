openai>=1.0,<2.0
flask>=3.0,<4.0
flask-cors>=4.0,<5.0
chromadb>=0.4.23,<1.0
requests>=2.28
beautifulsoup4>=4.12
lxml>=4.9
python-dotenv>=1.0,<2.0

# Python 3.10及以下需要pysqlite3-binary（解决SQLite版本过旧问题）
# Python 3.11+自带sqlite3 3.53+，无需安装
# pysqlite3-binary; python_version < "3.11"

# 可选依赖（按需安装）
# PyPDF2>=3.0        # PDF 解析
# python-docx>=1.0   # DOCX 解析
# fastembed>=0.2     # 本地 embedding 模型
